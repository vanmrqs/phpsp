{% include 'base/head.twig' %}

<main class="home">
    {% include 'base/menu.twig' %}

    <section class="section-principal">
        <h1> {% if not is_admin %} Meus {% endif %} Pagamentos </h1>

        {% include 'pagamento/partial_tabela.twig' %}
    </section>
</main>
